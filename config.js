/* config.js - the ONLY file you edit.
   Get both values from your Supabase project: Settings > API
   While these are placeholders the app runs in offline demo mode (data stays in your browser). */
window.CRICONFIG = {
  SUPABASE_URL: 'PASTE_YOUR_SUPABASE_PROJECT_URL',      // e.g. https://abcdefgh.supabase.co
  SUPABASE_ANON_KEY: 'PASTE_YOUR_SUPABASE_ANON_KEY'     // the "anon public" key - safe to ship, RLS protects writes
};
